<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.board.repository.FileMapper">

    <insert id="save">
        INSERT INTO file (name, real_name, board_id)
        VALUES (#{fileName}, #{realName}, #{boardId})
    </insert>

    <select id="findRealName" resultType="String">
        SELECT real_name
        FROM file
        WHERE board_id = #{boardId}
    </select>

    <select id="findFileNameByFileDTO" resultType="String">
        SELECT name AS fileName
        FROM file
        WHERE board_id = #{boardId}
          AND real_name = #{realName}
    </select>
</mapper>